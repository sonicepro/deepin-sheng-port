[Unit]
Description=3-finger swipe up -> show on-screen keyboard (onboard)
# NB: deliberately no "After=graphical.target". Combined with
# "WantedBy=multi-user.target" it forms an ordering cycle (graphical.target is
# ordered after multi-user.target), so systemd deleted the start job and the
# service never ran at all. The script tolerates starting early -- it just waits
# for the touchscreen and only calls onboard's D-Bus on a gesture.

[Service]
# Runs as the desktop user; reads the touchscreen directly and calls onboard's
# session-bus method org.onboard.Onboard.Keyboard.Show(). Uses the user's session
# bus explicitly (a system unit has no session env of its own).
Type=simple
User=luser
Environment=HOME=/home/luser
Environment=XDG_RUNTIME_DIR=/run/user/1000
Environment=DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus
ExecStart=/usr/bin/python3 /usr/local/sbin/sheng-gesture-keyboard.py
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
